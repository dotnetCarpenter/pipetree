{
  "name": "pipetree",
  "description": "Utility library to enable easy piping between nodejs programs and tree traversing of graphs",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "#pretest": "rollup --format cjs --input spec/integration/getZip.js --output spec/integration/getZip.bundle.js",
    "test": "tap spec/*.js --coverage"
  },
  "main": "lib/root.js",
  "bin": {
    "httpsget": "bin/httpsget.js"
  },
  "engines": {
    "node": ">=6"
  },
  "repository": "https://github.com/dotnetCarpenter/pipetree.git",
  "dependencies": {
    "follow-redirects": "^0.2.0"
  },
  "devDependencies": {
    "ecstatic": "^2.1.0",
    "tap": "^7.1.2",
    "thenify": "^3.2.1"
  },
  "nyc": {
    "include": [
      "lib",
      "bin"
    ]
  }
}
